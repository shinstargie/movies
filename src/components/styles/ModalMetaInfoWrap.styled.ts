import styled from "styled-components";
import { device } from "./_breakpoints";

const ModalMetaInfoWrap = styled.span`
  display: inline-block;
  font-size: 14px;
  margin: 0 5px;
  padding: 6px 10px;
  background-color: #b20600;
  color: white;
  border-radius: 5px;
`;

export default ModalMetaInfoWrap;
